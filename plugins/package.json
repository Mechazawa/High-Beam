{
    "name": "high-beam-plugins",
    "private": true,
    "workspaces": ["*"],
    "scripts": {
        "test": "npm test --workspaces --if-present"
    },
    "devDependencies": {
        "typescript": "^5.4.0",
        "vitest": "^4.1.7"
    }
}
